# Database
DATABASE_URL="postgresql://roomcast:roomcast@localhost:5432/roomcast"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Credential Encryption
ENCRYPTION_SECRET="generate-with-openssl-rand-base64-32"

# Sync Worker
CRON_SECRET="generate-with-openssl-rand-base64-32"

# Default Admin (for seeding)
ADMIN_EMAIL="admin@roomcast.local"
ADMIN_PASSWORD="changeme"

# --- OAuth Providers (optional) ---
# Google OAuth — create credentials at https://console.cloud.google.com/apis/credentials
# GOOGLE_AUTH_CLIENT_ID=""
# GOOGLE_AUTH_CLIENT_SECRET=""

# Microsoft Entra ID (Azure AD) OAuth — register app at https://portal.azure.com
# AZURE_AD_CLIENT_ID=""
# AZURE_AD_CLIENT_SECRET=""
# AZURE_AD_TENANT_ID=""

# --- LDAP (optional, requires ldapjs package) ---
# LDAP_URL="ldap://ldap.example.com:389"
# LDAP_BIND_DN_PREFIX="cn="
# LDAP_BIND_DN_SUFFIX=",ou=users,dc=example,dc=com"
# LDAP_SEARCH_BASE="ou=users,dc=example,dc=com"
